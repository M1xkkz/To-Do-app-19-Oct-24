<head>
  <style>
    /* ตั้งค่าทั่วไป */
    body {
      font-family: 'Arial', sans-serif;
      background: #f4f6f9;
      color: #333;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .edit-task-container {
      background-color: #fff;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 500px;
    }

    h1 {
      text-align: center;
      font-size: 2.2rem;
      margin-bottom: 20px;
      color: #34495e;
    }

    .form-group {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    .task-label {
      font-size: 1rem;
      color: #7f8c8d;
      font-weight: 600;
    }

    .task-input {
      padding: 12px;
      border: 2px solid #dcdde1;
      border-radius: 8px;
      font-size: 1rem;
      color: #333;
      transition: border-color 0.3s ease;
    }

    .task-input:focus {
      border-color: #3498db;
      outline: none;
    }

    .update-btn {
      background-color: #3498db;
      border: none;
      padding: 12px;
      color: white;
      border-radius: 8px;
      font-size: 1rem;
      cursor: pointer;
      transition: background-color 0.3s ease;
      font-weight: 600;
    }

    .update-btn:hover {
      background-color: #2980b9;
    }
  </style>
</head>

<body>
  <div class="edit-task-container">
    <h1>Edit Task</h1>

    <%= form_with model: @task, local: true do |f| %>
      <div class="form-group">
        <%= f.label :name, "Task Name", class: "task-label" %>
        <%= f.text_field :name, class: "task-input" %>
        <%= f.label :category_id, "Category", class: "task-label" %>
        <%= f.collection_select :category_id, @categories, :id, :name, prompt: "Select Category", class: "task-input" %>
        <%= f.submit "Update Task", class: "update-btn" %>
      </div>
    <% end %>
  </div>
</body>
